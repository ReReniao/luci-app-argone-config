name: Compile LuCI App (Argone Config - ARMv8)

on:
  push:
    branches: [ main ]
    paths:
      - '**'
  workflow_dispatch:

jobs:
  compile-armv8:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Plugin Source
        uses: actions/checkout@v4
        with:
          path: luci-app-argone-config

      # 关键1：拉取 OpenWrt 23.05 源码（确保架构配置文件完整）
      - name: Checkout OpenWrt Source (23.05)
        uses: actions/checkout@v4
        with:
          repository: openwrt/openwrt
          ref: openwrt-23.05
          path: openwrt
          fetch-depth: 10  # 拉取更多提交，确保架构配置文件不缺失
          submodules: false

      # 安装依赖（兼容工具链+基础依赖）
      - name: Install Compatible Dependencies & GCC-9
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++-9 gcc-9 gawk gcc-multilib gettext \
            git libncurses5-dev libncursesw5-dev libssl-dev python3 rsync unzip zlib1g-dev file wget libdeflate-dev
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-9 50
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-9 50
          gcc --version

      # 关键2：增加交换分区（避免工具链编译内存不足）
      - name: Add Swap Space
        run: |
          sudo fallocate -l 2G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          swapon --show

      - name: Configure OpenWrt Env & Copy Plugin
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          # 复制插件到编译目录
          cp -r ../luci-app-argone-config package/luci-app-argone-config

      # 关键3：直接加载 armvirt-64 架构的默认配置（彻底锁定架构）
      - name: Force ARMv8 (armvirt-64) Architecture
        run: |
          cd openwrt
          # 1. 彻底清理所有旧配置（关键！避免残留）
          rm -rf .config .config.old tmp/ staging_dir/ build_dir/
          # 2. 直接加载 armvirt-64 架构的默认配置（不依赖 defconfig 覆盖）
          cp target/linux/armvirt/64/config-5.15 .config  # 23.05 分支 armvirt-64 对应内核 5.15
          # 3. 勾选插件和依赖（不修改架构相关配置）
          echo "CONFIG_PACKAGE_luci-app-argone-config=y" >> .config
          echo "CONFIG_PACKAGE_luci-compat=y" >> .config
          # 4. 优化编译配置（关闭调试、减小体积）
          echo "CONFIG_DEBUG=n" >> .config
          echo "CONFIG_OPTIMIZE_FOR_SIZE=y" >> .config
          # 5. 生成最终配置（仅处理插件依赖，不改变架构）
          make oldconfig

      # 编译插件（单线程，确保工具链稳定构建）
      - name: Compile Plugin (ARMv8 Locked)
        run: |
          cd openwrt
          make package/luci-app-argone-config/compile V=s -j1

      # 上传产物（明确指定 ARMv8 架构路径）
      - name: Upload ARMv8 IPK
        uses: actions/upload-artifact@v4
        with:
          name: luci-app-argone-config-armv8-ipk
          path: openwrt/bin/packages/armvirt-64/luci/luci-app-argone-config_*.ipk
          retention-days: 30
